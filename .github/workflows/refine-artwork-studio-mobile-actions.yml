name: Refine artwork studio mobile actions

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/refine-artwork-studio-mobile-actions.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Apply Artwork Studio action hierarchy patch
        run: python .github/scripts/refine_artwork_studio_mobile_actions.py

      - name: Validate and commit patch
        run: |
          git diff --check
          git diff -- compass/index.html
          if git diff --quiet; then
            echo 'No changes needed.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add compass/index.html
          git commit -m 'Refine artwork studio mobile actions'
          git push origin HEAD:main
