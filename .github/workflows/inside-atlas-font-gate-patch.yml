name: Inside Atlas font gate patch

on:
  push:
    paths:
      - '.github/workflows/inside-atlas-font-gate-patch.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync current main
        run: |
          git fetch origin main
          git checkout main
          git reset --hard origin/main

      - name: Apply guarded Inside Atlas font gate
        run: python .github/atlas_patch_inside_fonts.py

      - name: Commit product change and remove runner
        run: |
          rm -f .github/workflows/inside-atlas-font-gate-patch.yml
          rm -f .github/atlas_patch_inside_fonts.py
          git config user.name "Atlas Patch Runner"
          git config user.email "atlas-patch@users.noreply.github.com"
          git add -A
          git diff --cached --check
          if git diff --cached --quiet; then
            echo "No product changes to commit."
            exit 0
          fi
          git commit -m "Gate Inside Atlas first paint on fonts"
          git push origin main
